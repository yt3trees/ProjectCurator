<UserControl x:Class="ProjectCurator.Views.Controls.CommandPaletteOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ProjectCurator.Views.Controls"
             xmlns:models="clr-namespace:ProjectCurator.Models;assembly=ProjectCurator.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Background="#80000000" MouseLeftButtonDown="OnBackgroundClick">
        <ui:Card VerticalAlignment="Top" HorizontalAlignment="Center" 
                 Margin="0,60,0,0" Width="600" Padding="0"
                 Background="{DynamicResource AppSurface0}"
                 BorderBrush="{DynamicResource AppSurface2}"
                 BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" MaxHeight="400" />
                </Grid.RowDefinitions>
                <ui:TextBox x:Name="SearchBox" Grid.Row="0"
                            PlaceholderText="Type a command..."
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                            Icon="{ui:SymbolIcon Symbol=Search24}"
                            Margin="8"
                            FontSize="16"
                            FontFamily="Consolas, Segoe UI"
                            PreviewKeyDown="OnSearchBoxKeyDown" />

                <ListBox x:Name="CommandList" Grid.Row="1"
                         ItemsSource="{Binding FilteredCommands}"
                         SelectedItem="{Binding SelectedCommand}"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         VirtualizingStackPanel.IsVirtualizing="True"
                         VirtualizingStackPanel.VirtualizationMode="Recycling"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:CommandItem}">
                            <Border Padding="12,8" Background="Transparent">
                                <TextBlock Text="{Binding Display}" 
                                           FontSize="14"
                                           FontFamily="Consolas, Segoe UI"
                                           Foreground="{DynamicResource AppText}" />
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent" SnapsToDevicePixels="true">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppSurface1}" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppSurface2}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </ui:Card>
    </Grid>
</UserControl>
